HMAC or other auth for rtsp links
Comand line processing
Logging
Auth lets not let anyone see our tivos
Make a video options module to guess a correct video size (User-Agent) add pram to rtsp link? At least keep the aspect ratio
Update the FFMpeg class to use the options
Stop using a hardcoded SPD and get it from ffmpeg some how
CSS (based on User-Agent? some regex matching system?)
Do everything the RTSP rfc says we should
Suport something other than RTP (UDP, TCP, HTTP Tunnel)
Make a cool android client with the json interface
